*.idea

/node_modules

test_db/*

/www
/redcap_docker

cypress.json
cypress.env.json
.DS_STORE

*.tmp
*.mp4
*.png

/tmp
/redcap_rsvc

cypress.config.js
cypress/downloads
cypress/videos
cypress/screenshots
cypress/results
# Ignore all test fixtures (provided locally / out-of-band, not committed).
# Keep the directory placeholder so cypress/fixtures/ exists on a fresh checkout.
cypress/fixtures/*
!cypress/fixtures/.gitkeep
!cypress/fixtures/.keep

# JUnit XML results (generated locally, regenerated by CI for the PR check)
/results

# Local parallel-shard run artifacts (run-ci.sh PARALLEL=1): per-shard results
# overlays, runner logs, and the unique per-shard spec lists.
/.shards
/.shard-logs
/shard-specs-*.txt

step-coverage/*
.nyc_output/*
cypress/fixtures/cdisc_files
cypress/fixtures/dictionaries
cypress/fixtures/import_files

cypress/features/Continue\ Last\ Run.feature

/containers
/docker
/find
/mysql

mysql-cli.zip

circleci-ssh-keys/
http-server.log
run-logs/

.verify_tmp/

# cypress_runner/ image: local run output + host-specific env (keep the tree,
# Dockerfile, compose, README, entrypoint, and flaky-tests.md tracked).
cypress_runner/results/
cypress_runner/.env
